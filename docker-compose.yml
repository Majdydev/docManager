version: "3.8"
services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./:/app
      - data:/app/data
    environment:
      - DATABASE_URL=file:./data/app.db
    command: sh -c "npm run build && npm run start"

volumes:
  data:
    driver: local
